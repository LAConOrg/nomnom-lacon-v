# NomNom Deployment Environment Configuration
# Copy this file to /etc/nomnom-deployment/deploy.env on the deployment host
# and customize the values for your environment.

# Deployment hostname (used for health checks)
# Production: nomnom.lacon.org
# Staging: nomnom-staging.lacon.org
DEPLOYMENT_HOSTNAME=nomnom.lacon.org

# Logging hostname (used for Tailscale/Dozzle)
# Production: nomnom
# Staging: nomnom-staging
LOGGING_HOSTNAME=nomnom

# Database configuration
# NOM_DB_USER should match the user in deploy/<stage>/pgbouncer/pgbouncer.ini
NOM_DB_USER=nomnom_user

# Optional: Export data path (defaults to /local if not set)
# EXPORT_DATA_PATH=/local
