# Example .env additions for webhook-based deployment

# NOMNOM_VERSION controls which Docker image tag to pull
# This is automatically set by the deployment script (deploy.sh)
# 
# Options:
#   main              - Latest build from main branch (default if not set)
#   sha-abc123        - Specific commit SHA (set by automated deployment)
#   pr-21             - Pull request build (for manual testing)
#
# IMPORTANT: The deployment script will automatically update this value.
# Only change it manually if you need to:
#   1. Test a specific PR: NOMNOM_VERSION=pr-123
#   2. Manually rollback: NOMNOM_VERSION=sha-abc123
#
# Example:
# NOMNOM_VERSION=sha-1a2b3c4

# Uncomment to override (otherwise defaults to 'main'):
# NOMNOM_VERSION=main

# DEPLOYMENT_HOSTNAME is used for the healthcheck Host header
# Production: DEPLOYMENT_HOSTNAME=nomnom.lacon.org
# Staging:    DEPLOYMENT_HOSTNAME=nomnom-staging.lacon.org
DEPLOYMENT_HOSTNAME=nomnom.lacon.org

# LOGGING_HOSTNAME is used for Tailscale logging sidecar and Dozzle
# Production: LOGGING_HOSTNAME=nomnom-lacon-production-logs
# Staging:    LOGGING_HOSTNAME=nomnom-lacon-staging-logs
LOGGING_HOSTNAME=nomnom-lacon-production-logs

# EXPORT_DATA_PATH is mounted in the scheduler container for data exports
# Defaults to /local if not specified
# EXPORT_DATA_PATH=/local

# Database configuration (used by both application and pgbouncer)
# NOM_DB_NAME, NOM_DB_USER, and NOM_DB_PASSWORD should already be set
# These additional variables are needed for pgbouncer.ini generation:
